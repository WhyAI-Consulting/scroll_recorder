{
  "name": "scroll-recorder",
  "version": "1.0.0",
  "description": "API service to record scrolling webpage videos",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.41.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
